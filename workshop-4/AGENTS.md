# Agent instructions

Project Agent Skills are stored canonically in `.agents/skills/<skill-name>/SKILL.md`.

When a task matches a skill description, read that skill's complete `SKILL.md` before acting and follow its instructions. Treat `.agents/skills` as the source of truth; do not create divergent copies of skills for individual agent products.
