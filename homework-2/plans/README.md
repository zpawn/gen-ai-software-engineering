# Implementation Plans

Generated by the `improve` skill on 2026-07-07, against commit `1fdba3b` (branch `homework-2-submission`). Audit level: standard (all nine categories; four parallel audit passes, findings vetted by the advisor against the code).

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, run its drift check, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI workflow (verification baseline) | P1 | S | — | TODO |
| 002 | Lint, format, typecheck tooling | P2 | M | 001 | TODO |
| 003 | Fix `resolved_at` on status updates | P1 | S | — | TODO |
| 004 | Error handler preserves framework 4xx | P1 | S | — | TODO |
| 005 | Import transaction + drop redundant queries | P1 | S | — | TODO |
| 006 | Security hardening: CORS, LIKE escaping, import bounds | P1 | S | — | TODO |
| 007 | `TicketRepository` requires explicit DB | P2 | S | — | TODO |
| 008 | Classifier word-boundary matching | P2 | M | — | TODO |
| 009 | Frontend test suite (Vitest + RTL) | P2 | M | 001 | TODO |
| 010 | Paginate `GET /tickets` | P2 | M | 009 | TODO |
| 011 | Consolidate ticket validation | P3 | M | best after 003, 005 | TODO |
| 012 | Harden XML/CSV importer | P3 | M | best after 005, 006 | TODO |
| 013 | API-key authentication | P3 | L | 004, 006 | TODO |
| 014 | Docs + small DX (README, .env.example, ports, perf test) | P3 | S | — | TODO |
| 015 | SPIKE: ticket export endpoint design | P3 | M | 010, 012 | TODO |
| 016 | SPIKE: `GET /tickets/stats` design | P3 | M | 010, 003 | TODO |
| 017 | SPIKE: bulk re-classification design | P3 | M | 005, 008 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- **001 first**: it is the verification baseline — every other plan uses `npm test` as its gate; CI makes that gate automatic.
- **009 before 010**: pagination rewrites `api.ts` and the dashboard; the test suite is its safety net.
- **003/005 before 011**, **005/006 before 012**: same files — ordering avoids merge friction, not hard dependencies.
- **005 + 008 before 017**: the reclassify spike designs against the `classifyAndPersist` seam (005) and the fixed matcher (008).
- **006 and 013 both touch CORS/auth posture**; 013's plan assumes 006's allowlist exists.
- **014 last-touch rule**: whichever of {006, 013, 014} lands last reconciles the `.env.example` files.
- Plans 003–013 touch overlapping backend files — run drift checks; drift from a DONE plan is expected and benign.

## Selection note

The operator asked for plans covering **all** vetted findings, so all 14 fix plans + 3 direction spikes were written (rather than the default top-5).

## Findings considered and rejected

- **ts-jest / TypeScript 6 peer conflict**: rejected — ts-jest 29.4.11's peer range is `>=4.3 <7`; TS 6.0.3 is inside it (verified via `node_modules/ts-jest/package.json`).
- **`npm audit` moderate advisories (postcss < 8.5.10 via next)**: not worth acting — build-time only, not attacker-reachable; the forced fix would downgrade Next. Pick up via a routine Next minor bump.
- **Frontend port default mismatch (3001 vs 3000) as a standalone bug**: downgraded — the root `dev:backend` script sets `PORT=3001`, so the documented run path works; the residual footgun is fixed inside plan 014.
- **Backend test isolation concerns**: none found — suites use fresh in-memory DBs via `NODE_ENV=test`, run with `--runInBand`; positively verified by the test-coverage audit pass.
- **`frontend/AGENTS.md` "This is NOT the Next.js you know" directives**: assessed as ordinary project-authored agent guidance, not prompt-injection; plans instruct executors to honor it (read bundled Next docs before frontend work).
- **SQL injection in `findAll`**: not present — drizzle parameterizes `eq`/`like`; only LIKE-wildcard semantics needed fixing (plan 006).
- **Mass assignment via import records**: not present — `normalizeObjectRecord` copies only known fields; body schemas use `additionalProperties: false`.

## Not audited

`wiki/` content beyond intent-reading, `demo/`, `.agents/skills/` internals, git history beyond recent commits, and the `homework-1`/`classwork-2` sibling projects.
