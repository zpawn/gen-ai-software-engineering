# Gemini / Antigravity Instructions

You are operating as Gemini (Antigravity IDE). 

**CRITICAL INSTRUCTION:** Your primary knowledge base schema, rules, and paths are defined in `AGENTS.md`. You **must** read `AGENTS.md` to understand how to operate wiki.

Agent-specific tips:
- Utilize your native file tools (`write_to_file`, `replace_file_content`, `multi_replace_file_content`) to create and update wiki pages incrementally.
- Keep in mind that `task.md` in your internal brain handles session state, but the wiki itself is persistent across sessions and visible to the user.
